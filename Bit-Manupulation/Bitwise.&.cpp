#include <iostream>
using namespace std;

int main(){

    int a, b, c;
    cin >> a >> b >> c;
    cout<<"And Operator :" << (a & b & c) << "\n";
    cout <<"OR operator :"<<(a | b | c )<<"\n";
    cout << "Xor is :" << (a ^ b ^ c) << "\n";
}